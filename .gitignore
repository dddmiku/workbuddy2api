# Auth & secrets
auths/
data/
*.key
*.pem
*.env
.env
.env.*

# Binaries (anchored to repo root only — bare "login"/"credit" would also ignore cmd/login/, cmd/credit/)
/login
/credit
/wb2api
/signin_bin

# Config with secrets (use config.example.json)
config.json

# Build artifacts
*.tmp
__pycache__/

# 真实会话测试载荷（私人对话内容，绝不入库）
real2.json
real_input.json
replay_full.json
combined.json
*.bak-*
build.log

# Backups (contains auth tokens)
backups/

# IDE
.idea/
.vscode/
LOOP.md
PLAN.md
SPEC.md

# 设计/计划文档（但 README.md 必须进版本库）

# Runtime credentials, backups, and diagnostic captures
/auths-trash/
/config.json.*
/artifacts/
*.tmp-*
*.bak
*.bak.*
*.bak-*
*.before-*
*.old.*
*.log
/real_input.json
/big_payload.json
**/*-wire-*.request.json
/trial_bin
/activity_bin
